# Database Configuration (REQUIRED)
# Main application database - stores users, valuations, auctions
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Soil Database (OPTIONAL but RECOMMENDED for performance)
# Separate PostgreSQL database for Iowa SSURGO soil data and CSR2 ratings
# Provides 10-100x faster CSR2 queries compared to external APIs
# If not set, application will use external APIs (slower but functional)
# See docs/Soil_Database_Setup_Guide.md for setup instructions
DATABASE_URL_SOIL=postgresql://user:password@host:port/soildb?sslmode=require

# OpenAI API Key (REQUIRED for AI valuations)
OPENAI_API_KEY=sk-...

# Firecrawl API Key (REQUIRED for auction scraping)
FIRECRAWL_API_KEY=fc-...

# Mapbox Access Token (REQUIRED for maps)
VITE_MAPBOX_PUBLIC_KEY=pk.eyJ1...

# Node Environment
NODE_ENV=development
